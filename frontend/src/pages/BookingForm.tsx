import React from 'react';
import { Container, Alert } from 'react-bootstrap';

const BookingForm: React.FC = () => {
  return (
    <Container>
      <Alert variant="info">
        <h4>Booking Form Page</h4>
        <p>This page is under development. It will allow users to book a specific room.</p>
      </Alert>
    </Container>
  );
};

export default BookingForm;