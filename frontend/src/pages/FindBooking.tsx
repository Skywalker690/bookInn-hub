import React from 'react';
import { Container, Alert } from 'react-bootstrap';

const FindBooking: React.FC = () => {
  return (
    <Container>
      <Alert variant="info">
        <h4>Find Booking Page</h4>
        <p>This page is under development. It will allow users to find their booking using confirmation code.</p>
      </Alert>
    </Container>
  );
};

export default FindBooking;